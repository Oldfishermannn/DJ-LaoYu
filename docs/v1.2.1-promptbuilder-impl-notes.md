# v1.2.1 PromptBuilder 实装笔记

> iOS Engineer · 2026-04-19 · branch `feature/v1.2.1-evolve-depth`（已 push）

## 交付

- **改了 4 个文件**：`PromptBuilder.swift`（+40 行）、`AppState.swift`（+88 / -14）、`StyleCategory.swift`（+76）、新增 `PromptState.swift`（+58）；xcodeproj 挂 PromptState。
- **Build**：4 次 `xcodebuild -sdk iphonesimulator build` 全过（Debug 签名 + validate pass）。
- **硬约束遵守**：未碰 LyriaClient / AudioEngine / Views / MusicStyle / AppState 核心状态机（仅 `evolve()` 方法体 + 新增 4 个 private 字段）。

## Commits（逻辑单元各一）

| Hash | 简述 |
|---|---|
| `247b157` | P1 · InstrumentPool 10 频道定义 + PromptPhrase 四档映射 + ScalarWalk 助手 |
| `7c24fe9` | P2 · PromptBuilder 三维度 build 重载（dedup 防 R2） |
| `dec207a` | P3 · evolve() 重写：1-2 维度 per tick + reshuffle + config density 同步 |
| `e237704` | P4 · temperature 默认 1.1 → 0.8（CEO 决策 #1 A 方案） |

## RFC 对齐 / 偏差

- 严格按 RFC §2.1–§2.5 伪码，**无二次创作**。
- 决策 #2（density/energy 不暴露 UI）遵守：4 个字段全 `private`。
- 决策 #3（灰度 fallback 不预购）遵守：未实装 UserDefaults 开关。
- 可逆性：`EvolveVocabulary.variant` + `PromptBuilder.evolveVariant` 保留不删。
